export class Product {
    code: string; // Assuming the code is a string, adjust as needed
    category: string;
    name: string;
    imagePath: string;
    price: number;
    minQuantity: number;
    discountRate: number;
}
